import { RevStrPipePipe } from './rev-str-pipe.pipe';

describe('RevStrPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RevStrPipePipe();
    expect(pipe).toBeTruthy();
  });
});
